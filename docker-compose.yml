version: '3.7'

services: 
  smart-brain-api:
    container_name: backend
    # image: node:carbon
    build: ./
    command: npm start
    working_dir: /usr/src/smart-brain-api
    ports: 
      - "3000"